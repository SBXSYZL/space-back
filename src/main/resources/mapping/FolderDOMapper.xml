<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.demo.dao.FolderDOMapper">
    <resultMap id="BaseResultMap" type="com.project.demo.DO.FolderDO">
        <id column="folder_id" jdbcType="INTEGER" property="folderId"/>
        <result column="folder_name" jdbcType="VARCHAR" property="folderName"/>
        <result column="author_id" jdbcType="INTEGER" property="authorId"/>
        <result column="parent_folder_id" property="parentFolderId"/>
        <result column="create_date" property="createDate"/>
    </resultMap>
    <sql id="Base_Column_List">
    folder_id, folder_name,author_id,parent_folder_id,create_date
  </sql>
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from folder
        where folder_id = #{folderId,jdbcType=INTEGER}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from folder
    where folder_id = #{folderId,jdbcType=INTEGER}
  </delete>
    <insert id="insert" parameterType="com.project.demo.DO.FolderDO">
    insert into folder (folder_id, folder_name)
    values (#{folderId,jdbcType=INTEGER}, #{folderName,jdbcType=VARCHAR})
  </insert>
    <insert id="insertSelective" parameterType="com.project.demo.DO.FolderDO">
        insert into folder
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="folderId != null">
                folder_id,
            </if>
            <if test="folderName != null">
                folder_name,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="folderId != null">
                #{folderId,jdbcType=INTEGER},
            </if>
            <if test="folderName != null">
                #{folderName,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.project.demo.DO.FolderDO">
        update folder
        <set>
            <if test="folderName != null">
                folder_name = #{folderName,jdbcType=VARCHAR},
            </if>
        </set>
        where folder_id = #{folderId,jdbcType=INTEGER}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.project.demo.DO.FolderDO">
    update folder
    set folder_name = #{folderName,jdbcType=VARCHAR}
    where folder_id = #{folderId,jdbcType=INTEGER}
  </update>

    <insert id="createFolder">
        insert into folder(folder_name,author_id,parent_folder_id)
        values(#{path,jdbcType=VARCHAR},#{userId,jdbcType=INTEGER},#{parentFolderId,jdbcType=INTEGER})
    </insert>
    <select id="getFilesUnderFolderId" resultType="com.project.demo.VO.FileVO">
        select  folder_id as fileId,
                parent_folder_id as folderId,
                folder_name as fileName,
                author_id,
                create_date
        from folder
        where author_id=#{userId,jdbcType=INTEGER}
        and parent_folder_id=#{folderId,jdbcType=INTEGER}
    </select>
    <delete id="deleteDir">
        delete from folder
        where folder_id=#{folderId,jdbcType=INTEGER}
    </delete>
</mapper>